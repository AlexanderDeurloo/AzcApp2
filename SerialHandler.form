# Gambas Form File 2.0

{ Form Form
  MoveScaled(70,26.4286,38,17)
  Text = ("Serial Port")
  { SPort #SerialPort
    #X = 189
    #Y = 7
    FlowControl = 0
    PortName = "/dev/ttyS0"
    Speed = 9600
  }
  { Label2 Label
    MoveScaled(2,2,24,3)
    Text = ("All the settings are in here:")
  }
  { TimerOutSerial #Timer
    #X = 56
    #Y = 63
  }
}
